/* eslint-disable @next/next/no-img-element */

import { OpenGraphImageProps } from "@/types";

import { BaseOGImage } from "./base";

const OGImage = ({}: OpenGraphImageProps) => {
  return <BaseOGImage />;
};

export default OGImage;
